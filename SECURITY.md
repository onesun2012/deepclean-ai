# 安全策略

DeepClean 是本地 Windows 清理工具。误删问题使用仓库的误删 Issue 模板；安全漏洞请通过仓库 Security Advisories 或仓库所有者私下报告，不要公开敏感路径或会话凭据。

## 当前实现边界

- 默认只读扫描，普通权限下由用户确认后清理。管理员实例禁止实际清理和清空回收站；系统变更命令已移除。
- 全部 `/api/` 接口需要启动时随机凭据，同时校验 Host，变更请求继续校验 Origin 和 Fetch-Site。凭据通过浏览器 URL fragment 引导并存入当前标签页 sessionStorage，不写入日志或公开 HTML。它是持有凭据的会话认证，不是 Windows 身份认证，也不隔离同账户恶意进程。
- 网页使用一次性后端预览计划，五分钟有效，重新扫描后失效；新的预览替代旧预览。CLI 的每次调用重新扫描，以该次 `--yes` 明确授权的类别为范围。
- 锁定/模型类别不能清理，重叠规则中的受保护目录也会排除。Gemini tmp、Cursor workspaceStorage、Codex runtime/应用状态和未验证的宽泛更新目录暂时锁定；PyTorch 权重仅统计。
- 扫描拒绝根、祖先和子项的符号链接/重解析点。清理前复核文件身份、大小、纳秒修改/创建时间、路径归属和当前保留期。变更文件跳过；排除支持整个子树，Windows 路径不区分大小写；不再移除空目录。
- 扫描与清理使用进程内互斥锁，停止期间不可重入，异常释放锁。同用户、同安装路径、同权限模式下，文件锁使网页/CLI 实例互斥；不同安装路径和普通/管理员模式分别协调，不宣称系统范围的所有副本互斥。
- 实例发现文件使用 Windows DPAPI 加密，重复启动解密并验证已有服务的会话后再打开页面；退出需要会话凭据，正在执行任务时拒绝退出，不按端口强杀程序。DPAPI 不隔离同账户恶意进程。
- 按分类或分项扫描，未扫描分项不进入计划；全局最多保留 150,000 条清单，估算预算 96 MiB（不是 RSS 硬上限）。超限标记 partial 并拒绝清理，需缩小范围重扫。
- 普通清理使用 `SHFileOperationW + FOF_ALLOWUNDO`，错误不回退为永久删除。该接口不能保证所有介质/系统配置均可回收，也没有逐项完成回调；返回数字属于 Shell 成功报告的逻辑大小，不代表实际磁盘释放空间。界面独立读取前后可用空间，该变化也可能受其他进程影响。
- 清空回收站只能单独执行，是不可撤销操作。
- 迁移目前仅预览第一个明确模型目录；旧版复制、总字节数校验、junction 和自动删源实现已移除。需完成事务日志、逐文件校验和恢复验收后再开放。
- 测试变量 `CLEAR_C_SANDBOX` 将扫描限制到测试类别，永久删除适配器还会检查每个路径位于测试根内且无重解析点。`DEEPCLEAN_PERMANENT` 不再生效；非 Windows 不自动永久删除。
- 只监听 127.0.0.1，无遥测。历史留在本机，包含汇总与少量样例路径，不是备份或逐文件恢复功能。

## 尚未消除的风险

Windows Shell 接口接受字符串路径；最终复核到实际回收之间仍存在竞态。本轮不能声称彻底消除普通权限下的 TOCTOU。管理员只读切断了本工具的高权限变更通道。完整身份绑定需要评估稳定句柄/可信暂存与回收兼容性，未完成前不建议把本工具用作对抗同账户恶意进程的隔离设施。

现有回收测试使用 mock；临时 junction 替换测试验证拒绝已发生的替换，不证明原子删除。源码和 Windows x64 EXE 候选包已通过隔离进程生命周期测试（换端口、重复启动、限定扫描、资源和认证退出）。尚未完成真实 Shell 回收行为、32 位 Windows、跨用户对抗或崩溃恢复的验证。

## 旧版本迁移残留

若旧版迁移留下 `.deepclean-bak`，先保留源、目标和备份，不要直接删除任何一份。核对实际目录联接目标并比较文件清单和内容后，再选择恢复；本版本不会自动接管旧迁移事务。
