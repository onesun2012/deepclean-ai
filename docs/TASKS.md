# 项目完善任务清单

本轮目标：先把本地清理工具的数据保护、操作一致性和测试隔离做好，继续保持零运行时依赖。验收仅使用临时目录，不扫描或清理真实用户数据。

## 本轮实施

- [x] 收紧可能包含会话、应用状态和模型权重的清理规则。
- [x] 为全部本地 API 增加启动会话凭据、严格请求校验与读取超时。
- [x] 管理员模式只读；迁移统一改为预览，移除未充分校验的删源实现。
- [x] 扫描拒绝重解析路径；清理复核文件身份、修改时间、根归属和保留期。
- [x] 沙盒永久删除严格限定到测试根；取消清理空目录的旁路。
- [x] 扫描和清理互斥，停止期间禁止重入；异常能结束任务并释放锁。
- [x] 锁定/模型目录分项仅保留统计，减少清单内存；执行每 64 项分批并允许取消。
- [x] 修复单项勾选、当前页面操作范围、一次性后端确认计划和空间结果展示。
- [x] 支持作者入口改为配置驱动，未配置不显示收款占位。
- [x] 增加隔离回归测试和 Windows CI 配置，更新使用说明及安全边界。
- [x] 完成独立修复复审，修复其发现的三个问题，记录测试结果和剩余限制。

## 本轮验收（2026-09-07）

- 本机 Windows / Python 3.11：规则 19 项、HTTP 15 项、回收适配器 14 项、安全回归 14 项、隔离 CLI 4 项，共 66 项通过。
- Edge 无头浏览器夹具：会话引导、单项勾选、当前视图范围、后端计划提交、磁盘真实前后值、未配置支持入口隐藏，以及空间接口失败仍保留结果，均通过；无浏览器脚本错误。API 全部为夹具，不运行真实清理。
- Python 编译检查、`git diff --check` 通过。CI 已配置，未声称远端 CI 已运行。
- 独立复审发现并修复：受限扫描的 roots 接口报错、清理期间可生成并继续执行旧计划、磁盘读取失败导致结果丢失；同时增加执行响应丢失后的同计划状态恢复。
- 没有扫描/清理真实用户数据，没有打包发布 EXE，也没有进行真实回收站或迁移验收。性能改进依据数据结构和执行方式，不声称已测得实际提速。
- 原审查报告保留为历史基线，当前功能与剩余风险以 README 和 SECURITY.md 为准。

## 后续版本（不冒充本轮已实现）

- [ ] 迁移事务日志、逐文件内容校验、崩溃恢复及 Windows 实机验收完成后恢复迁移执行。
- [ ] 调研文件句柄绑定/可信暂存与可靠回收站接口，消除最后校验到 Shell 路径操作之间的竞态。
- [x] 按分类/单工具/CLI 分项扫描；全局清单条目与估算内存预算；超限禁止清理。
- [ ] 真实 SSD/HDD 基准测试与进程 RSS 验证。
- [x] 同安装路径、同用户和权限模式的实例协调，以及受认证的安全退出。
- [x] 固定构建依赖、生成构建输入清单与 SHA256，完成源码和 EXE 候选包的隔离生命周期验收。
- [ ] 跨机器逐字节构建复现、代码签名及公开发行。
- [x] 接入作者提供的微信/支付宝原图，以及 GitHub Sponsors、Ko-fi、Afdian 链接；明确免费使用、自愿支持。
- [x] 制作带模拟数据标记的四张截图、约 30 秒演示视频、发布文案与字幕建议。
- [ ] 按审查报告的四周零现金方案小范围推广（尚未对外发送或发布）。

## 第二轮交付

候选包：`dist/candidate/DeepClean.exe`；校验值与构建输入同目录保存。推广素材及文案：`docs/promotion/`。发行范围和限制见 `docs/RELEASE-CANDIDATE.md`。

本轮新增范围/预算测试和真实进程生命周期验收，验证均使用临时目录。构建使用 Windows 10 x64、Python 3.11.9、PyInstaller 6.22.2，包未签名也未上传。旧版全扫描与端口强杀启动/退出脚本已替换。

最终验证：68 项后端/CLI 回归通过，另有源码、EXE 两套真实进程生命周期验收和浏览器交互验收通过。构建清单输入摘要与当前源码一致，EXE 的 SHA256 与附带校验文件一致。演示视频约 34 秒，已检查截图与视频抽帧，画面明确标注模拟数据。

推广入口与后续优先级详见 `REVIEW-2026-09-06.md`。不在修复期间投放广告或引入付费服务。
